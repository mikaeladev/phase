buildImage = "oven/bun:1.1.42"

[phases.setup]
aptPkgs = ["build-essential", "python3"]

[phases.install]
dependsOn = ["setup"]
cmds = ["bun install"]

[phases.build]
dependsOn = ["install"]
cmds = ["bun run build:bot"]

[start]
runImage = "oven/bun:1.1.42"
cmd = "bun run start:bot"
